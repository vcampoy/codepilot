# codepilot Project Context

## Detected Stack

- No application package manifest or source tree was found.
- Repository content is currently documentation-only plus SDD bootstrap artifacts.
- `.gitignore` is Python-oriented, but no Python code or tooling files were detected.

## Conventions

- Markdown is the only visible content format in the repo.
- SDD bootstrap lives under `openspec/`.
- Skill indexing lives under `.atl/skill-registry.md`.

## Architecture

- No runtime architecture can be inferred yet.
- No modules, services, or package boundaries were detected.

## Testing

- No test runner, coverage tool, linter, formatter, or type checker was detected.
- Strict TDD mode is disabled because there is no runnable test stack yet.
