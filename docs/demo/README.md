# Demo capture

Run the local stack, open `http://localhost:5173`, submit a public HTTPS repository, and capture the completed overview and analysis-history views. Store review screenshots here only after removing repository names, credentials, tokens, and private source content. The current API intentionally renders evidence-pending empty states when a data endpoint is not available; do not replace them with invented metrics.
